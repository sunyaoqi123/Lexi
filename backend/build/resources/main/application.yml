spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lexi?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&useUnicode=true
    username: root
    password: syq78575
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

server:
  port: 8081
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

jwt:
  secret: lexi-jwt-secret-key-2024-must-be-at-least-256-bits-long
  expiration: 604800000  # 7 days in ms
